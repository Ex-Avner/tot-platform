services:
  - type: web
    name: tot-platform
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: TOT_COACH_PASSWORD
        generateValue: false
        value: avner2025
      - key: PYTHON_VERSION
        value: 3.11.0
